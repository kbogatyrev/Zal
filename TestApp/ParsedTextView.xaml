<UserControl x:Class="ZalTestApp.ParsedTextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:ZalTestApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="20,60,0,0" Padding="10,2,10,5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
        <Border BorderBrush="Black" BorderThickness="1" Margin="0,0,0,0" >
<!--
            <xctk:RichTextBox Name="ParsedText" Text="{Binding ParsedText}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Padding="10,10,10,10" />
-->
            <TextBlock Name="ParsedText" Text="{Binding ParsedText}"/>
        </Border>
    </ScrollViewer>

</UserControl>
