<Window x:Class="ZalTestApp.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZalTestApp"
        mc:Ignorable="d"
        Title="Zal Test App" Height="700" Width="1050" Margin="0">

   
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
<!--        
        <DataTemplate DataType="{x:Type local:LexemeViewModel}">
            <Grid Margin="30,30,30,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="121*"/>
                    <ColumnDefinition Width="159*"/>
                    <ColumnDefinition Width="82*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Style="{DynamicResource NameField}" Text="Исходная форма" />
                <TextBlock Style="{DynamicResource NameField}" Text="Графическая основа" Grid.Row="1"/>
                <TextBlock Style="{DynamicResource NameField}" Text="Основной символ" Grid.Row="2"/>
                <TextBlock Style="{DynamicResource NameField}" Text="Индекс" Grid.Row="3"/>
                <TextBlock Style="{DynamicResource NameField}" Text="Схема ударения" Grid.Row="4"/>
                <TextBlock Style="{DynamicResource NameField}" Text="Доп. информация" Grid.Row="5"/>

                <TextBlock Text="{Binding Path=SourceForm}" Grid.Column="1"/>
                <TextBlock Text="{Binding Path=GraphicStem}" Grid.Row="1" Grid.Column="1"/>
                <TextBlock Text="{Binding Path=MainSymbol}" Grid.Row="2" Grid.Column="1"/>
                <TextBlock Text="{Binding Path=Index}" Grid.Row="3" Grid.Column="1"/>
                <TextBlock Text="{Binding Path=StressSchema}" Grid.Row="4" Grid.Column="1"/>
                <TextBlock Text="{Binding Path=Advanced}" Grid.Row="5" Grid.Column="1"/>
            </Grid>
        </DataTemplate>
-->
<!-- 
https://docs.microsoft.com/en-us/dotnet/framework/wpf/advanced/how-to-define-a-table-with-xaml        
-->        
    </Window.Resources>

    <ContentControl Content="{Binding CurrentViewModel}" />

</Window>
